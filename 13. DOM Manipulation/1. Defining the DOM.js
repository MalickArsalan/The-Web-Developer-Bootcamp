//          DOM
// The DOM -> Document Object Model
// The Document Object Model is the interface between your Javasscript and GTML +CSS

// The browser turns every HTML tag into a Javscript object that we can manipulate
// Everything is stored inside of the document object

console.dir(docoment);